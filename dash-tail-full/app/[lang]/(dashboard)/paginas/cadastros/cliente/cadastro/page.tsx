import { ClienteFormContent } from './content';

export const metadata = {
  title: 'Cadastro de Cliente - Tremonte',
  description: 'Cadastro e edição de clientes',
};

export default function ClienteCadastroPage() {
  return <ClienteFormContent />;
}

