import { ClienteContent } from './content';

export const metadata = {
  title: 'Clientes - Tremonte',
  description: 'Gerenciamento de clientes',
};

export default function ClientePage() {
  return <ClienteContent />;
}
